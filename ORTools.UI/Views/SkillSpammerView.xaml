<UserControl x:Class="ORTools.UI.Views.SkillSpammerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ORTools.UI.ViewModels" xmlns:helpers="clr-namespace:ORTools.UI.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=vm:SkillSpammerViewModel}">

    <UserControl.Resources>
        <Style x:Key="SpammerCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="IsThreeState" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Bd" Width="16" Height="16" BorderBrush="#313338" BorderThickness="1" Background="White" CornerRadius="0">
                            <TextBlock x:Name="CheckMark" Text="✓" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Hidden" FontWeight="Bold" FontSize="12" Margin="0,-2,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0078D7"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0078D7"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckMark" Property="Text" Value="✓"/>
                                <Setter TargetName="CheckMark" Property="FontSize" Value="12"/>
                                <Setter TargetName="CheckMark" Property="Margin" Value="0,-2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Bd" Property="Background" Value="#DB950D"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#DB950D"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckMark" Property="Text" Value="-"/>
                                <Setter TargetName="CheckMark" Property="FontSize" Value="18"/>
                                <Setter TargetName="CheckMark" Property="Margin" Value="0,-4,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="SpammerKeyTemplate" DataType="{x:Type vm:SpammerKeyViewModel}">
            <StackPanel Orientation="Horizontal" Margin="4">
                <CheckBox Style="{StaticResource SpammerCheckBoxStyle}" 
                          IsChecked="{Binding IsCheckedState, Mode=TwoWay}"
                          VerticalAlignment="Center" Margin="0,0,4,0"/>
                <Image Source="{Binding ImagePath}" Width="24" Height="24" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Keys Area -->
        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,20,0">
            <ItemsControl ItemsSource="{Binding AllKeys}" ItemTemplate="{StaticResource SpammerKeyTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="9" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>

        <!-- Right Panel (Legend & Delay) -->
        <StackPanel Grid.Column="1" Grid.Row="0" Width="160">
            <GroupBox Header="Legend" Margin="0,0,0,10" Foreground="{StaticResource AppTextBrush}" BorderBrush="{StaticResource AppBorderBrush}">
                <StackPanel Margin="5">
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <CheckBox Style="{StaticResource SpammerCheckBoxStyle}" IsChecked="True" IsHitTestVisible="False" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="With click" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}" Width="70"/>
                        <Image Source="pack://application:,,,/Icons/Spammer/spammer_mouse_on.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <CheckBox Style="{StaticResource SpammerCheckBoxStyle}" IsChecked="{x:Null}" IsHitTestVisible="False" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="No click" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}" Width="70"/>
                        <Image Source="pack://application:,,,/Icons/Spammer/spammer_mouse_off.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <CheckBox Style="{StaticResource SpammerCheckBoxStyle}" IsChecked="False" IsHitTestVisible="False" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Disabled" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}" Width="70"/>
                        <Image Source="pack://application:,,,/Icons/Spammer/spammer_off.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>


            <GroupBox Header="Spammer Delay" Foreground="{StaticResource AppTextBrush}" BorderBrush="{StaticResource AppBorderBrush}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <TextBox Text="{Binding SpammerDelay, UpdateSourceTrigger=PropertyChanged, Delay=500}"
                             helpers:InputHelper.IsNumericInput="True"
                             PreviewKeyDown="SpammerDelay_PreviewKeyDown"
                             TextChanged="SpammerDelay_TextChanged"
                             Style="{StaticResource NumericTextBoxStyle}" Width="50" />
                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="{StaticResource AppTextBrush}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Bottom Panel (Key Config) -->
        <GroupBox Grid.Row="1" Grid.ColumnSpan="2" Header="Key Config" Margin="0,10,0,0" Foreground="{StaticResource AppTextBrush}" BorderBrush="{StaticResource AppBorderBrush}">
            <StackPanel Orientation="Horizontal" Margin="10,5">
                <CheckBox Content="Mouse Flick" IsChecked="{Binding MouseFlick}" Foreground="{StaticResource AppTextBrush}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Image Source="pack://application:,,,/Icons/Spammer/up_and_down_arrow.png" Width="16" Height="16" Margin="0,0,20,0" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" VerticalAlignment="Center"/>
                
                <CheckBox Content="No Shift" IsChecked="{Binding NoShift}" Foreground="{StaticResource AppTextBrush}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Image Source="pack://application:,,,/Icons/Spammer/shift.png" Width="16" Height="16" Margin="0,0,20,0" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" VerticalAlignment="Center"/>
                
                <CheckBox Content="Toggle Mode" IsChecked="{Binding ToggleMode}" Foreground="{StaticResource AppTextBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding ToggleModeKey, Mode=TwoWay}"
                         PreviewKeyDown="ToggleModeKey_PreviewKeyDown" ToolTip="Press a key to assign"
                         Style="{StaticResource KeyTextBoxStyle}" HorizontalAlignment="Left" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
