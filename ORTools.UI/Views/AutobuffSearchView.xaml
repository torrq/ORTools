<UserControl x:Class="ORTools.UI.Views.AutobuffSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ORTools.UI.ViewModels"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        
        <DataTemplate x:Key="SlotItemTemplate">
            <Grid Margin="4,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="24" Height="24" Background="Transparent" CornerRadius="2" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Image Source="{Binding ImagePath}" Stretch="None"
                           RenderOptions.BitmapScalingMode="NearestNeighbor" SnapsToDevicePixels="True"/>
                </Border>

                <TextBox Grid.Column="1" Text="{Binding Key, Mode=TwoWay, Converter={StaticResource KeyToStringConverter}}"
                         PreviewKeyDown="KeyTextBox_PreviewKeyDown"
                         Style="{StaticResource KeyTextBoxStyle}" 
                         Height="24" FontSize="12" Width="50" Margin="0,0,6,0" VerticalAlignment="Center" />

                <TextBlock Grid.Column="2" Text="{Binding DisplayName}" VerticalAlignment="Center"
                           TextWrapping="Wrap" LineHeight="12"
                           Foreground="{DynamicResource AppTextBrush}" 
                           FontSize="{Binding DisplayName, Converter={StaticResource TextLengthToFontSizeConverter}}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="16,0,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <Border Grid.Row="0" BorderThickness="1" BorderBrush="{DynamicResource AppBorderBrush}" CornerRadius="4" Background="{DynamicResource AppInputBrush}" Margin="0,0,0,4" Padding="4,4,4,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Search:" Foreground="{DynamicResource AppSubtleBrush}" VerticalAlignment="Center" Margin="4,0,8,0"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Background="Transparent" BorderThickness="0" Foreground="{DynamicResource AppTextBrush}" 
                         VerticalAlignment="Center" Padding="0,2"/>
            </Grid>
        </Border>

        <!-- Search Results -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel>
                <!-- Active Slots Section -->
                <StackPanel Visibility="{Binding HasActiveSlots, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource AppBorderBrush}" Margin="0,0,0,10" Padding="0,0,0,4">
                        <TextBlock Text="Active Slots" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AppTextBrush}" />
                    </Border>
                    <ItemsControl ItemsSource="{Binding ActiveSlots}" ItemTemplate="{StaticResource SlotItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" ItemWidth="215" ItemHeight="34" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>

                <!-- Search Results Section -->
                <StackPanel Visibility="{Binding HasSearchResults, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,15,0,0">
                    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource AppBorderBrush}" Margin="0,0,0,10" Padding="0,0,0,4">
                        <TextBlock Text="Search Results" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AppTextBrush}" />
                    </Border>
                    <ItemsControl ItemsSource="{Binding SearchResults}" ItemTemplate="{StaticResource SlotItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" ItemWidth="215" ItemHeight="34" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
