<UserControl x:Class="ORTools.UI.Views.AutopotSPView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ORTools.UI.ViewModels" xmlns:helpers="clr-namespace:ORTools.UI.Helpers"
             Background="Transparent">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="54"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="38"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="ON" Foreground="{DynamicResource AppSubtleBrush}" FontWeight="SemiBold" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Icons/tab_autopot_sp.png" Width="10" Height="12" Stretch="None" Margin="0,0,4,0" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock Text="KEY" Foreground="{DynamicResource AppSubtleBrush}" FontWeight="SemiBold" FontSize="10" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Grid.Column="2" Text="SP %" Foreground="{DynamicResource AppSubtleBrush}" FontWeight="SemiBold" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        <!-- Slots -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Slots}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="54"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="38"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox IsChecked="{Binding Enabled}" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center">
                        </CheckBox>

                        <TextBox Grid.Column="1" Text="{Binding Key, Mode=TwoWay, Converter={StaticResource KeyToStringConverter}}"
                                 PreviewKeyDown="KeyTextBox_PreviewKeyDown"
                                 Style="{StaticResource KeyTextBoxStyle}" HorizontalAlignment="Center" Width="44" Height="24" FontSize="12" />

                        <Slider Grid.Column="2" Value="{Binding Percent, Delay=150}" Minimum="0" Maximum="100"
                                VerticalAlignment="Center" Margin="6,0,6,0"
                                IsSnapToTickEnabled="True" TickFrequency="1" />

                        <TextBox Grid.Column="3" Text="{Binding Percent, UpdateSourceTrigger=PropertyChanged, Delay=500}" 
                                 Style="{StaticResource NumericTextBoxStyle}" Width="36" Height="24" FontSize="12"
                                 VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Settings -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Center">
            <TextBlock Text="Delay (ms):" Foreground="{DynamicResource AppSubtleBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Text="{Binding Delay, UpdateSourceTrigger=PropertyChanged, Delay=500}" 
                     helpers:InputHelper.IsNumericInput="True"
                     Style="{StaticResource NumericTextBoxStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
