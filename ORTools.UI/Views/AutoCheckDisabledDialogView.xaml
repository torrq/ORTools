<UserControl x:Class="ORTools.UI.Views.AutoCheckDisabledDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ORTools.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance vm:AutoCheckDisabledDialogViewModel}"
             Background="Transparent">

    <Border Background="{DynamicResource AppSurfaceBrush}" 
            BorderBrush="{DynamicResource AppBorderBrush}" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="24"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">

        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" Direction="270" ShadowDepth="5"/>
        </Border.Effect>

        <StackPanel MinWidth="280">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                <TextBlock FontFamily="pack://application:,,,/Fonts/#lucide" Text="&#xE05A;" FontSize="20" Foreground="{DynamicResource AppWarningBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="{DynamicResource S.Dialog.AutoCheckDisabledTitle}" Foreground="{DynamicResource AppTextBrush}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock TextAlignment="Center" Foreground="{DynamicResource AppTextBrush}" FontSize="14" Margin="0,0,0,24" TextWrapping="Wrap" MaxWidth="360" HorizontalAlignment="Center">
                <Run Text="{DynamicResource S.Dialog.AutoCheckDisabledDesc1}"/>
                <Hyperlink Command="{Binding GoToUpdatesCommand}" Foreground="{DynamicResource AppLinkBrush}" TextDecorations="None">
                    <Run Text="{DynamicResource S.Dialog.AutoCheckDisabledLink}"/>
                </Hyperlink>
                <Run Text="{DynamicResource S.Dialog.AutoCheckDisabledDesc2}"/>
            </TextBlock>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="OK" Command="{Binding CloseCommand}" 
                        IsDefault="True"
                        IsCancel="True"
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Width="100" Height="32"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
