<UserControl x:Class="ORTools.UI.Views.AutoOffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Column: Primary Settings -->
        <StackPanel Grid.Column="0">
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16" VerticalAlignment="Center">
                <Image gif:ImageBehavior.AnimatedSource="pack://application:,,,/Icons/ui/clock_animated.gif" 
                       Width="32" Height="32" Margin="0,0,12,0" />
                <TextBlock Text="Auto Off Settings" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock Text="Conditions" Foreground="{StaticResource AppMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8" />
            
            <!-- Overweight -->
            <Border Background="{StaticResource AppSurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <CheckBox Content="Auto Off on Overweight" IsChecked="{Binding AutoOffOverweight}" VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" IsEnabled="{Binding AutoOffOverweight}">
                        <RadioButton Content="50%" IsChecked="{Binding AutoOffOverweightMode, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=50}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <RadioButton Content="90%" IsChecked="{Binding AutoOffOverweightMode, Converter={StaticResource EqualityToBooleanConverter}, ConverterParameter=90}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Kill Client -->
            <Border Background="{StaticResource AppSurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <CheckBox Content="Kill Game Client on Auto Off" IsChecked="{Binding AutoOffKillClient}" />
            </Border>

            <!-- Auto Off Timer -->
            <Border Background="{StaticResource AppSurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Auto Off Timer (minutes)" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}"/>
                    <TextBox Grid.Column="1" Text="{Binding AutoOffTime, UpdateSourceTrigger=PropertyChanged}" Width="50" Height="24" Background="{StaticResource AppInputBrush}" Foreground="{StaticResource AppTextBrush}" BorderBrush="{StaticResource AppInputBorderBrush}" VerticalContentAlignment="Center" Padding="4,0" />
                </Grid>
            </Border>
        </StackPanel>

        <!-- Right Column: Keys & Ammo -->
        <StackPanel Grid.Column="2">
            <TextBlock Text="Auto Off Keys" Foreground="{StaticResource AppMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8" />
            
            <!-- Off Keys -->
            <Border Background="{StaticResource AppSurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Auto Off Key 1:" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableKeys}" SelectedItem="{Binding AutoOffKey1}" Width="80" Height="24" />

                    <TextBlock Grid.Row="2" Text="Auto Off Key 2:" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AvailableKeys}" SelectedItem="{Binding AutoOffKey2}" Width="80" Height="24" />
                </Grid>
            </Border>

            <TextBlock Text="Ammo Switch" Foreground="{StaticResource AppMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8" />
            
            <!-- Ammo Switch -->
            <Border Background="{StaticResource AppSurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox Content="Enable Ammo Switch" IsChecked="{Binding SwitchAmmo}" Grid.ColumnSpan="2" />

                    <Grid Grid.Row="2" Grid.ColumnSpan="2" IsEnabled="{Binding SwitchAmmo}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Ammo 1 Key:" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableKeys}" SelectedItem="{Binding Ammo1Key}" Width="80" Height="24" />
                    </Grid>

                    <Grid Grid.Row="4" Grid.ColumnSpan="2" IsEnabled="{Binding SwitchAmmo}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Ammo 2 Key:" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableKeys}" SelectedItem="{Binding Ammo2Key}" Width="80" Height="24" />
                    </Grid>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
