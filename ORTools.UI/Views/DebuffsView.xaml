<UserControl x:Class="ORTools.UI.Views.DebuffsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ORTools.UI.ViewModels" xmlns:helpers="clr-namespace:ORTools.UI.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Recovery Row -->
        <ItemsControl ItemsSource="{Binding StatusRecoveryItems}" HorizontalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10,0,10,0">
                        <!-- Icon -->
                        <Image Source="{Binding ImagePath}" Stretch="None" Margin="0,0,8,0" VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="NearestNeighbor" SnapsToDevicePixels="True"/>
                        
                        <!-- Name -->
                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" 
                                   Foreground="{DynamicResource AppTextBrush}" FontSize="14" Margin="0,0,8,0"/>

                        <!-- Key -->
                        <TextBox Text="{Binding Key, Mode=TwoWay, Converter={StaticResource KeyToStringConverter}}"
                                 PreviewKeyDown="KeyTextBox_PreviewKeyDown"
                                 Style="{StaticResource KeyTextBoxStyle}" 
                                 Height="28" FontSize="14" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Separator -->
        <Border Grid.Row="1" Height="1" Background="{DynamicResource AppBorderBrush}" Margin="0,16"/>

        <!-- Debuffs Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DebuffItems}" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" ItemWidth="220" ItemHeight="46" Margin="0,10,0,0" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding ImagePath}" Stretch="None" VerticalAlignment="Center" Margin="0,0,6,0"
                                   RenderOptions.BitmapScalingMode="NearestNeighbor" SnapsToDevicePixels="True"/>

                            <TextBox Grid.Column="1" Text="{Binding Key, Mode=TwoWay, Converter={StaticResource KeyToStringConverter}}"
                                     PreviewKeyDown="KeyTextBox_PreviewKeyDown"
                                     Style="{StaticResource KeyTextBoxStyle}" 
                                     Height="24" FontSize="12" Width="50" Margin="0,0,6,0" VerticalAlignment="Center" />

                            <TextBlock Grid.Column="2" Text="{Binding DisplayName}" VerticalAlignment="Center"
                                       TextWrapping="Wrap" LineHeight="12"
                                       Foreground="{DynamicResource AppTextBrush}" 
                                       FontSize="{Binding DisplayName, Converter={StaticResource TextLengthToFontSizeConverter}}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Settings -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Center">
            <TextBlock Text="Delay (ms):" FontSize="14" Foreground="{DynamicResource AppSubtleBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Text="{Binding Delay, UpdateSourceTrigger=PropertyChanged, Delay=500}" 
                     helpers:InputHelper.IsNumericInput="True" helpers:InputHelper.NumericDefault="50"
                     Style="{StaticResource NumericTextBoxStyle}" Width="70" Height="28" FontSize="14" />
        </StackPanel>
    </Grid>
</UserControl>
