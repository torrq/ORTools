<UserControl x:Class="ORTools.UI.Views.MiscView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:ORTools.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MiscViewModel}"
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid Margin="12">
        <GroupBox Header="Transfer Helper">
            <StackPanel Margin="12">
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Transfer Key:" VerticalAlignment="Center" Foreground="{StaticResource AppTextBrush}" />
                    <TextBox Grid.Column="1" Text="{Binding TransferKey, Converter={StaticResource KeyToStringConverter}}"
                             PreviewKeyDown="TransferKey_PreviewKeyDown"
                             Style="{StaticResource KeyTextBoxStyle}" HorizontalAlignment="Left" />
                </Grid>
                <TextBlock Text="Hover over an item and press this key to instantly transfer it (simulates Alt + Right Click). Works between Inventory, Storage, and Cart."
                           TextWrapping="Wrap" Foreground="{StaticResource AppSubtleBrush}" FontSize="11" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
